services:
  db:
    image: mysql:8.0
    container_name: ecb_db
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: ecb
      MYSQL_USER: ecb_user
      MYSQL_PASSWORD: secret
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data:
