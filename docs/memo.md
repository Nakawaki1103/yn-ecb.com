# メモ

> 気になったこと・あとで調べたいこと・思いついたことをここに書く

## PHPのコードを読むコツ

**`$this` の読み方**
1. クラスの上を見て `class 〇〇` を確認
2. PHPDocの `@property` でカラム一覧を確認
3. `$this->カラム名` で何を参照しているかわかる

**メソッドの返り値**
- メソッド名の横の `: bool` / `: string` などで返ってくる型がわかる
- 返り値の型がわかれば、そのメソッドが何をしているか予想できる
